---
title: "Go1.25〜1.26のコンパイラはスライスをスタックに置きたがる"
emoji: "🪑"
type: "tech"
topics:
  - "go"
  - "コンパイラ"
  - "最適化"
published: false
---

## 導入

本記事はGo公式ブログの の解説したものになります。

公開当時に目を通していたものの、後半があまりキャッチアップできておらず、改めて整理した上で手元で確かめて理解を深めようと思い、執筆しました。

---

**Go1.26にかけて、コンパイラが段階的にスライスのバッキングストアをスタックに置きに行くようになっており**、そのロジックが興味深かったので、まとめてみました。

段階的に最適化が導入されており、それぞれ「どういう形のコードを想定していて、どの制約を乗り越えた結果生まれたのか」という物語になっていて読み応えがあったので、このブログでは1つ1つ整理しながら見ていきたいと思います！

```bash
❯ go version
go version go1.26.2 darwin/arm64 # 筆者の手元のバージョン
```

:::message
補足
バッキングストアは [Allocating on the Stack](https://go.dev/blog/allocation-optimizations) では「backing store」として言及されおり、スライスの基底配列のことを指しています。
:::

## この記事で知れること

- Go 1.26までに段階的に導入されたスライスのスタック割り当て最適化の中身

## 前提のおさらい: スタック割り当てとヒープ割り当ては何が違うのか

まずはスタックとヒープの違いを軽く整理しておきます。メモリは大きく分けるとこの2つに置かれます。

**スタック割り当て**は以下のような性質を持ちます。

- 関数の終了とともにメモリが自動で解放されるため、GC (ガベージコレクタ) の追跡対象にならない
- 関数呼び出しのたびに領域が再利用される
- メモリ上で連続しているので、CPUキャッシュの効率が良い

一方、**ヒープ割り当て** は以下の性質を持ちます。

- 割り当てられたメモリはGCが管理し、不要になればGCが回収する
- GCの実行頻度や負荷が増えると、アプリケーション全体のパフォーマンスに影響する

---

Goのコンパイラは開発者が書いた `new(T)` や `make([]T, ...)` のようなコードを、必ずヒープに置くわけではありません。**エスケープ解析 (escape analysis)** を行い、その変数が関数の外に「逃げない」と判断できれば、スタックに置いてくれることがあります。

「エスケープする」というのは、変数への参照が関数の外 (呼び出し元や別のゴルーチン、ヒープ上の構造体の中など) に渡ってしまい、関数終了後もメモリが生き残っている必要がある、という意味です。逃げてしまうと関数終了時に解放してはいけないので、ヒープに置くしかありません。

今回の最適化は、エスケープしないスライスをより広くスタックに載せられるようにするだけでなく、最終的にエスケープする場合でも途中段階をスタックで処理してヒープ割り当てを減らす方向に進んでいる。

## 出発点の問題: スライスの「スタートアップフェーズ」のオーバーヘッド

まずは以下のようなコードから考えてみます。

```go
func process(c chan task) {
	var tasks []task
	for t := range c {
		tasks = append(tasks, t)
	}
	processAll(tasks)
}
```

チャネルから流れてくる `task` を受け取って、すべて受け取り終わったら `processAll` に渡す、という処理です。

しかし、このコードは内部的には以下のような動きをしています。

1. 最初の `append` で容量1のバッキングストアがヒープに割り当てられる
2. 2回目の `append` で容量が足りなくなり、容量2の新しいバッキングストアがヒープに割り当てられる。古い容量1のバッキングストアはガベージになる
3. 3回目の `append` で容量4のバッキングストアが新たに割り当てられ、容量2の方はガベージになる
4. 以降、8、16、32... と倍々に拡張されていく

記事ではこの中間のアロケーションの連なりを「**スタートアップフェーズ (startup phase)**」と呼んでいて、最終的な容量にたどり着くまでに発生する無駄なヒープアロケーションの群れ、と説明されています。

ここからの4つの最適化は、この「スタートアップフェーズのオーバーヘッドをどうやって削るか」という内容になります。

## 出発点: 容量が定数の `make` をスタックに置く

以前から入っている最適化は、**「容量が定数の `make` で作られたスライスのバッキングストアを、エスケープしなければスタックに置く」** というものです。

つまり、開発者が「おそらく最大10個くらいだろう」と見積もって、以下のように書いたコードが対象になります。

```go
func process2(c chan task) {
	tasks := make([]task, 0, 10) // おそらく最大10個のタスク
	for t := range c {
		tasks = append(tasks, t)
	}
	processAll(tasks)
}
```

コンパイラは以下の2つの条件を満たしたときにバッキングストアをスタックに置きます。

1. **容量が定数である** (この例では10)
2. **バッキングストアが `processAll` 内部からヒープにエスケープしない** (エスケープ解析で確認される)

この2つが満たされれば、`process2` 関数内でのヒープアロケーションは0回になります。もちろん、容量10を超えて `append` してしまうとバッキングストアの拡張が必要になり、その時点でヒープへのフォールバックが発生します。

:::message
この最適化の対象はあくまで「容量が**定数**のとき」です。たとえば引数で渡ってきた変数を容量に指定した場合は対象外です。その制約を取り払いにいくのが次のGo 1.25になります。
:::

### 実際にベンチマークを計測してみた

Go 1.23 ~ Go 1.26

| シナリオ                            | 条件 |                    Go 1.23 |             Go 1.24 |             Go 1.25 |             Go 1.26 |
| ----------------------------------- | ---- | -------------------------: | ------------------: | ------------------: | ------------------: |
| **1: 定数 make** `make([]int,0,10)` | —    | 9.07ns / 0B / **0 allocs** | 9.18ns / 0B / **0** | 9.27ns / 0B / **0** | 9.12ns / 0B / **0** |

TODO: もっとみやすくする

## Go 1.25: 容量が変数でも32バイトのスタックバッファを確保する

容量を引数で指定できるようにした、以下のコードを考えます。

```go
func process3(c chan task, lengthGuess int) {
	tasks := make([]task, 0, lengthGuess)
	for t := range c {
		tasks = append(tasks, t)
	}
	processAll(tasks)
}
```

`lengthGuess` は実行時にしかわからないので、これまでの最適化は使えません。ここで「じゃあコンパイル時に動的にスタックフレームのサイズを変えればいいのでは？」と思いたくなりますが、記事中には以下のような一節があります。

> All Go stack frames are fixed size. There is no equivalent of C's `alloca`.

(意訳)
Goのスタックフレームはすべて固定サイズである。Cの `alloca` のような仕組みは存在しない。

**Cのように動的にスタックフレームを伸ばすことはできない**、というのが大前提なんですね。

---

これを踏まえてGo 1.25のコンパイラがとった方針は、**「コンパイル時に32バイトの小さなスタックバッファをスタックフレームに埋め込んでおき、実行時に要求サイズに応じて使い分ける」** というものです。

大まかな流れは以下になります。

1. コンパイラが `make` の呼び出し箇所に、32バイトのスタックバッファを事前に確保するコードを差し込む
1. 実行時に `lengthGuess` が評価され、要求サイズが32バイト以内であればスタックバッファを使う
1. 要求サイズが32バイトを超える場合は、従来通りヒープにフォールバックする

ポイントは、**スタックフレームは依然として固定サイズのまま**だという点です。32バイトという小さな固定サイズのバッファをあらかじめ埋め込んでおくことで、「動的にスタックフレームを伸ばせない」という制約を回避しています。

## Go 1.26

### その1: `append` 呼び出し箇所にもスタックバッファを置く

Go 1.25までの最適化は、**`make` で明示的に容量を指定するケース** が対象でした。でも冒頭のスタートアップフェーズの例 (`process` 関数) のように、`var tasks []task` とだけ宣言して `append` で積んでいく書き方は、Goではかなり一般的だと思います。

Go 1.26では、**`append` の呼び出し箇所自体にスタックバッファを埋め込む最適化** が追加されました。

動作のイメージは以下の通りです。

1. 最初の数回の `append` では、スタック上のバッファにデータが書き込まれる
2. スタックバッファが満杯になったら、それ以降はヒープに大きめの容量を確保してそちらを使う
3. スタックバッファに収まる程度の小さなスライスであれば、ヒープアロケーションは一切発生しない

これで、冒頭の `process` のような「一切 `make` を書いていないコード」でも、**開発者が何もしていないのにスタートアップフェーズのオーバーヘッドが削減される**、という嬉しい状態になります！

### その2: エスケープするスライスと `runtime.move2heap()`

ここまでの最適化には共通の前提条件がありました。それは **「バッキングストアが関数の外にエスケープしないこと」** です。

でも、関数の戻り値としてスライスを返すケースはどうでしょうか？

```go
func extract(c chan task) []task {
	var tasks []task
	for t := range c {
		tasks = append(tasks, t)
	}
	return tasks
}
```

この場合、返り値として外に持ち出される以上、バッキングストアは必ずヒープ上になければいけません。関数が終了しても生き残っていないといけないからです。これまでの話だと、ここはまだ最適化の対象外です。

---

従来、このケースを手で最適化しようとすると、以下のように **最終サイズがわかった時点で1回だけ `make` + `copy` する** パターンがよく使われていました。

```go
func extract2(c chan task) []task {
	var tasks []task
	for t := range c {
		tasks = append(tasks, t)
	}
	tasks2 := make([]task, len(tasks))
	copy(tasks2, tasks)
	return tasks2
}
```

でも、このパターンには地味に欠点があります。**`tasks` がたとえ小さくてスタックバッファに収まるサイズであっても、最後の `make` + `copy` は必ず実行されてしまう**ことです。その分、余計なヒープアロケーションとコピーが発生してしまうわけですね。

---

そもそも「なぜ `make` + `copy` が最適化になるのか？」というと、**エスケープ解析の対象を `tasks` と `tasks2` に分けている**のがポイントです。

素朴な `extract` では `tasks` 自体が戻り値として外に逃げるので、コンパイラは append の途中経過のバッキングストアも含めて全部ヒープに置くしかありません。スタートアップフェーズで cap 1 → 2 → 4 → 8 → 16 と拡張される過程のゴミが丸ごとヒープに発生してしまいます。

一方 `extract2` では、`tasks` は関数内で `copy` の読み取り元にしか使われず、戻り値にもなりません。**エスケープするのは `tasks2` だけ**なので、コンパイラは「`tasks` のバッキングストアは関数内で閉じる」と判断できます。その結果、

1. append の途中経過 (スタートアップフェーズの中間バッキングストア) が Go 1.25以降であればスタック上の32バイトバッファで賄える
2. 最後の `make([]task, len(tasks))` で **ちょうど `len` ぴったりのサイズ** を1回だけヒープ確保する (倍々で膨らんだ余分な capacity を切り詰められる)
3. `copy` でスタック → ヒープにデータを移して返す

という流れになり、「**エスケープする範囲を最終結果だけに絞ることで、途中経過をスタックに押し込める**」手動最適化として成立していたわけです。

---

Go 1.26のコンパイラは、これを別のアプローチで自動化するようになってます。具体的には、**戻り値の直前に `runtime.move2heap()` の呼び出しをコンパイラが自動で挿入する**、という表現が公式ブログで用いられています。`runtime.move2heap()` はブログで _"a special compiler+runtime function"_ と説明されている、通常のGo関数とは異なる関数です。

具体的には、以下のように `return` の手前に `runtime.move2heap()` の呼び出しが自動で挿入されていると考えてください。

```go
func extract3(c chan task) []task {
	var tasks []task
	for t := range c {
		tasks = append(tasks, t)
	}
	tasks = runtime.move2heap(tasks) // コンパイラが同等の処理を自動で挿入
	return tasks
}
```

差し込まれる `runtime.move2heap()` は以下のように動作します。

1. **渡されたスライスがすでにヒープ上にある場合**: 何もせずそのまま返す (恒等関数として動作)
2. **スライスがスタック上にある場合**: 正確なサイズのヒープ領域を1回だけ割り当て、データをコピーしてヒープ上のスライスを返す

**`extract2` の手動パターンとの違い**は、「スライスがすでにヒープにある場合には何もしない」という点です。

- スタックバッファに収まるケース: ループ中はスタック上で積まれ、最後に1回だけヒープに正確なサイズでコピーされる
- スタックバッファに収まらず、途中でヒープにあふれたケース: 最後の `runtime.move2heap()` は何もせず、既存のヒープバッキングストアがそのまま使われる

:::message
`extract2` と `extract3` は似ているように見えますが、「バッキングストアがすでにヒープにある場合に、余分な `make` + `copy` が発生するかどうか」という点で挙動が違います。コンパイラによる自動挿入の方が賢い動きをしてくれる、と捉えるとわかりやすいかなと思います。
:::

## もし最適化で問題が起きたら

万が一これらの最適化が原因で、既存のプログラムがおかしな挙動になってしまった場合の話です。記事中には、以下のフラグで無効化できると記載されています。

```bash
-gcflags=all=-d=variablemakehash=n
```

以前、Go 1.22のfor loop仕様変更に関するブログを書いた際にも、影響範囲を特定するためのフラグ (`-gcflags=all=-d=loopvar=2`) が用意されていましたが、Goチームは **コンパイラの最適化を入れるときに、何かあったときの逃げ道をちゃんと用意してくれる** 印象があります。

https://zenn.dev/kkkxxx/articles/d1a14c19359eec

## まとめ

Go 1.26にかけて段階的に入ったスライスのスタック割り当て最適化について、整理してみました。

個人的に印象的だったのは以下の3点です。

- **開発者がコードを変更することなく、コンパイラが自動的に恩恵をくれる**という点。Goらしい「普通に書けば速い」思想を感じる
- **「Goのスタックフレームは固定サイズ」という制約のもとで、32バイトの小さなバッファを埋め込むというアプローチ**で可変サイズに対応していること。制約の中で工夫している感じがおもしろい
- **`runtime.move2heap()` が手動パターンよりも賢い**こと。手動最適化よりもコンパイラの方がうまくやれるケースがちゃんとある

GoのプロダクトでGCの負荷やアロケーション数を気にされている方にとっては、アップデートの恩恵を自然に受けられる嬉しい変更になりそうです！

### 参考文献

- [The Go Blog: Allocating on the Stack](https://go.dev/blog/allocation-optimizations)
- [A Guide to the Go Garbage Collector](https://go.dev/doc/gc-guide)
- [Go FAQ: How do I know whether a variable is allocated on the heap or the stack?](https://go.dev/doc/faq#stack_or_heap)
